import type { ReactNode } from "react";

export const metadata = {
  title: "WEBIC - reset password",
  description: "reset password"
};

export default function SignInLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

import type { ReactNode } from "react";

export const metadata = {
  title: "WEBIC - Welcom to web ide service home",
};

export default function WelcomeLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

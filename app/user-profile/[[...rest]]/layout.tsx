import type { ReactNode } from "react";

export const metadata = {
  title: "WEBIC - User Profile",
};

export default function WelcomeLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
